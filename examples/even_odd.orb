invoke <stdio.tome>

spell conjure() => arcanum {
    //Gathering mana...

    arcanum amount <= 0;
    scry(stdin, @amount@, 3);

    evoke amount % 2;
}

// Take in value from stdin
// If even, return "Dark"
// If odd, return "Light"
