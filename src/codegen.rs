/**
 * TODO: the codegen accepts an AST (repr. by a vec of Stmts) and
 * returns LLVM IR representing the machine code of the program.
 * From there, common tools can be used to optimize & compile
 * to system architecture.
 */
