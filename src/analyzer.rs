/**
 * TODO: The semantic analyzer takes in an AST (vec of stmts)
 * and deduces if they represent a valid program (declaration before use,
 * single definitions for values, etc.) and then throws an error or
 * returns a false value or something if it does not. Anything
 * that gets past semantic analysis should be a valid
 * program ready to be sent to the codegen stage.
 */
