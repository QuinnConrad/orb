spell call_foo() => halfling {
  evoke 2.0;
}

spell call_bar() => void {
  evoke;
}

spell fireball(shmungus level) => void {
  # gathering mana
  arcanum a;
  halfling b <= 4.0;
  mytype c <= b + call_foo(); #cast spell and capture.
  call_bar(); #cast spell and do not capture.
}



# end
